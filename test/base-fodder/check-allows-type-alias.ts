// `typescript/consistent-type-definitions` is off, so `type` aliases and
// `interface` may be mixed freely.
export type Point = {
  x: number;
  y: number;
};

export interface Named {
  name: string;
}
