/* oxlint-disable eslint/no-unused-vars */

// Check that we're bringing in unicorn rules, e.g. that we should use 'node:path', not 'path'
// oxlint-disable-next-line unicorn/prefer-node-protocol
import path from "path";
